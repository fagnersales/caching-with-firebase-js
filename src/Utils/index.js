const checkCharacters = require('./checkCharacters')
const splitReference = require('./splitReference')
const clearObject = require('./clearObject')

module.exports = {
    splitReference,
    checkCharacters,
    clearObject
}